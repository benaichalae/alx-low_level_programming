#include "main.h"
/**
 * mul -multiplies two integers
 * @a: int to be multiply with b
 * @b: int to be multiply with a
 * Return: the value of multiplie a with b
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
